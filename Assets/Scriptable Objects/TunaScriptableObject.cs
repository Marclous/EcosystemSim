using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "TunaData", menuName = "EcosystemGame/Data/TunaData", order = 1)]
public class TunaScriptableObject : ScriptableObject
{
    // Start is called before the first frame update
    public int hitPoint;

    public float lifespan;
    // Start is called before the first frame update
    public void TunaMovement() {


    }
}
